<%= form_for(@domain) do |f| %>
  <% if @domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
      <% @domain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hostname %><br />
    <%= f.text_field :hostname %>
  </div>
  <div class="field">
    <%= f.label :pagerank %><br />
    <%= f.text_field :pagerank %>
  </div>
  <div class="field">
    <%= f.label :domainage %><br />
    <%= f.date_select :domainage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
